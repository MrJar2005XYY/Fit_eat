# 轻食刻 — 功能实现清单

> 基于代码实际审查，记录每个功能的真实实现状态。

---

## 一、已完整实现的功能（18 项）

前后端打通，使用真实数据，可正常使用。

| # | 功能 | 涉及文件 | 说明 |
|---|------|----------|------|
| 1 | 用户注册 | auth.py, login.html | 账号+邮箱+密码，重复校验，密码哈希存储 |
| 2 | 用户登录 | auth.py, login.html | 密码验证，session 管理，返回 token |
| 3 | 用户退出 | auth.py, common.js | 清除 session 和 localStorage |
| 4 | 查看/编辑个人资料 | user.py, profile.html | 昵称/头像/简介/性别/年龄/身高/体重/体脂/目标 |
| 5 | 修改账号 ID | user.py, profile.html | 唯一性校验，最少 3 字符 |
| 6 | 修改密码 | user.py, profile.html | 旧密码验证，新密码哈希，最少 6 位 |
| 7 | 头像更换 | user.py, profile.html | 默认头像（dicebear）+ 文件上传 |
| 8 | 添加饮食记录 | diet.py, home.html, common.js | 食物搜索 + 选择餐次 + 提交记录 |
| 9 | 查看今日卡路里 | diet.py, home.html | 已摄入/目标/剩余，环形图展示 |
| 10 | 查看今日餐单 | diet.py, home.html | 按时间排序的今日饮食记录列表 |
| 11 | 删除饮食记录 | diet.py, home.html | 仅限本人记录 |
| 12 | 周卡路里图表 | diet.py, record.html | 本周 7 天每日卡路里柱状图 |
| 13 | 饮水追踪 | diet.py, record.html | 记录饮水杯数（250ml/杯），实时更新 |
| 14 | 食物列表浏览 | food.py, discover.html | 分页加载，按餐次筛选（早/午/晚/加餐） |
| 15 | 食物搜索 | food.py, discover.html | 实时搜索 + 防抖，支持本地 fallback |
| 16 | 食物详情 | food.py, food-detail.html | 名称/图片/营养/食材/步骤/标签 |
| 17 | 社区发帖 | community.py, community.html | 文字+图片+分类+定位，真实上传 |
| 18 | 社区点赞 | community.py, community.html | 切换点赞/取消，实时更新计数 |
| 19 | 我的动态 | community.py, my-posts.html | 查看/删除本人帖子，分页加载 |
| 20 | 成就展示 | achievement.py, profile.html | 展示所有成就，区分已解锁/未解锁 |
| 21 | 图片上传 | upload.py | UUID 文件名，类型校验（png/jpg/gif/webp） |
| 22 | 管理后台 | admin/, /admin | 9 个模型视图，搜索/过滤/导出 |
| 23 | 数据库初始化 | init_db.py | 2 用户 + 6 食物 + 8 成就 + 示例数据 |

---

## 二、部分实现的功能（6 项）

有 UI 或有后端，但前后端未完全打通，或使用了硬编码/占位数据。

### 2.1 宏量营养素追踪（碳水/脂肪）

- **现状**：DietRecord 模型只存 calories 和 protein，不存 carbs 和 fat
- **后端**：`GET /api/diet/macros` 返回 carbs=0, fat=0（硬编码）
- **前端**：record.html 显示三条进度条，但碳水和脂肪永远为 0
- **需修复**：DietRecord 模型添加 carbs/fat 字段，添加记录时一并存储

### 2.2 营养雷达图

- **现状**：6 个维度中只有 energy 和 protein 从真实数据计算
- **后端**：`GET /api/diet/nutrition-radar` 中 fat/fiber/vitamins/minerals 为硬编码值（72/65/80/75）
- **前端**：雷达图组件正常渲染，但数据不真实
- **需修复**：基于饮食记录计算各维度真实占比

### 2.3 食物收藏

- **现状**：后端 `POST /api/food/<id>/favorite` 直接返回 success，不存储任何数据
- **前端**：food-detail.html 收藏按钮仅切换图标，不调用 API
- **需修复**：创建 Favorite 模型，前后端对接

### 2.4 社区评论

- **现状**：Comment 模型已定义，`POST /api/community/posts/<id>/comment` 可创建评论
- **缺失**：没有 `GET` 端点读取评论列表
- **前端**：community.html 显示"评论功能开发中..."
- **需修复**：添加 GET 评论接口，前端实现评论列表和输入框

### 2.5 社区关注

- **现状**：Follow 模型已定义，`POST /api/community/follow/<user_id>` 可切换关注
- **Bug**：前端传的是 `post.id` 而非 `post.user.id`（关注了帖子而非用户）
- **缺失**：没有关注者/被关注者列表页面，没有粉丝数展示
- **需修复**：修正前端传参，添加关注列表页面

### 2.6 AI 身体数据提交

- **现状**：后端 `POST /api/achievement/ai/body-data` 正常工作，可存储数据并计算 BMI
- **前端**：ai-setup.html 只存 localStorage，不调用后端 API
- **需修复**：前端提交时调用 `API.ai.submitBodyData()`

---

## 三、未实现的功能（18 项）

UI 中存在入口但无实际逻辑，或完全没有代码。

### 3.1 AI 相关（核心缺失）

| # | 功能 | 现状 | 说明 |
|---|------|------|------|
| 1 | AI 饮食方案生成 | 后端返回硬编码数据 | `GET /api/achievement/ai/plan` 返回固定的 4 餐食谱，未接入任何 AI API（无 OpenAI/Claude/文心等） |
| 2 | AI 方案应用 | 后端仅 +1 计数 | `POST /api/achievement/ai/apply` 只给 plan_days +1，不生成饮食记录 |
| 3 | AI 健康建议 | 前端硬编码文字 | record.html 底部"AI健康建议"是固定文案，不根据用户数据变化 |
| 4 | AI 营养师建议 | 前端硬编码文字 | food-detail.html 的"AI营养师建议"是固定文案 |

### 3.2 成就系统

| # | 功能 | 现状 | 说明 |
|---|------|------|------|
| 5 | 成就自动解锁 | 完全未实现 | Achievement 模型有 condition_type 和 condition_value，但没有任何代码判断条件并解锁成就 |

### 3.3 社区功能

| # | 功能 | 现状 | 说明 |
|---|------|------|------|
| 6 | 查看评论列表 | 无后端接口 | 可以创建评论但无法查看 |
| 7 | 分享功能 | 按钮存在但无逻辑 | 社区和食物详情页的分享按钮显示"开发中" |
| 8 | 社区加载更多 | 按钮存在但无逻辑 | 点击只显示 toast，不加载下一页 |
| 9 | 社区搜索 | 图标存在但无逻辑 | 顶部搜索图标无点击事件 |

### 3.4 记录页

| # | 功能 | 现状 | 说明 |
|---|------|------|------|
| 10 | 日期切换加载数据 | UI 存在但无逻辑 | 日期选择器渲染正常，但切换日期不重新请求数据 |
| 11 | 时间段切换 | 按钮存在但无逻辑 | "本周/上周/本月"按钮无事件处理 |
| 12 | 综合评分/等级 | 硬编码 HTML | 评分 88、等级 A-、状态"良好"是固定值 |

### 3.5 发现页

| # | 功能 | 现状 | 说明 |
|---|------|------|------|
| 13 | 食材百科 | 硬编码 6 条数据 | 鸡胸肉/牛油果/三文鱼等，不从 API 加载 |
| 14 | 健康知识文章 | 硬编码标题 | 点击无反应，无文章详情页 |
| 15 | 每日一贴 | 从 5 条硬编码数组随机 | 不个性化，不更新 |
| 16 | 热门标签 | 静态 HTML | 点击不触发搜索或筛选 |

### 3.6 其他

| # | 功能 | 现状 | 说明 |
|---|------|------|------|
| 17 | 通知系统 | 图标存在但无功能 | 多个页面有通知铃铛图标，纯装饰 |
| 18 | 管理后台权限 | 无鉴权 | `is_accessible()` 始终返回 True，任何人可访问 /admin |

---

## 四、已知 Bug

| # | Bug | 位置 | 影响 |
|---|-----|------|------|
| 1 | 社区关注传参错误 | community.html line 240 | `followUser(post.id)` 应为 `followUser(post.user.id)`，关注了帖子而非用户 |
| 2 | 食物详情"加入今日饮食"无效 | food-detail.html | 按钮只显示 toast，不创建饮食记录 |
| 3 | 食物收藏纯前端 | food-detail.html | 图标切换但不调用 API，刷新后丢失 |
| 4 | 推荐文章硬编码 | home.html | 底部"推荐文章"是固定图片和文字 |

---

## 五、优先级建议

### P0 — 必须修复（影响核心体验）
1. DietRecord 添加 carbs/fat 字段，宏量营养素真实计算
2. 营养雷达图 6 维度全部使用真实数据
3. AI 方案页接入真实 AI API（至少用模板算法替代硬编码）
4. 成就自动解锁逻辑

### P1 — 应该修复（功能完整性）
5. 评论查看接口 + 前端评论列表
6. 食物收藏功能对接
7. 社区关注 bug 修复
8. AI 身体数据页调用后端 API
9. 日期切换加载历史数据
10. 食物详情"加入今日饮食"功能对接

### P2 — 可以优化（体验提升）
11. 社区加载更多（分页）
12. 社区搜索功能
13. 分享功能
14. 发现页内容动态化
15. 管理后台添加鉴权
